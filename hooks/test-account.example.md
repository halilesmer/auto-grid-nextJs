# Test hesabı (DEMO) — şablon

Bu dosyayı `test-account.local.md` olarak kopyalayıp doldurun. `.local.md` gitignore'ludur ve
asla commit'lenmez. **Şifre buraya yazılmaz.** Claude, "test et" denince bu hesabı worker'da
zaten kayıtlı hesap olarak seçer (protokol: `hooks/RULES.md` §3).

- **Tür:** DEMO
- **Hesap ID (worker `accounts.json` içindeki id):** <örn. 12345678>
- **MT5 login no:** <örn. 12345678>
- **Sunucu:** <örn. BrokerName-Demo>
- **Motor adı:** <örn. Auto_Grid>
- **Worker:** VPS + ngrok (URL `frontend_nextjs/.env.local` içinde `NEXT_PUBLIC_API_URL`)
- **Notlar:** <test edilecek sembol, beklenen zone/ayarlar vb.>
